export const pluralMap: Record<string, string> = {
    colors: 'color',
    screens: 'screen',
    fontSize: 'text',
    borderRadius: 'rounded',
    boxShadow: 'shadow',
}

export type CssMapType = { name: string; value: string }
